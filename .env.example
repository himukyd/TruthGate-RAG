# --- LLM Configuration ---
# Optional: Set to 'local' to use Qwen2.5 (default) or provide a key for cloud models
LLM_TYPE=local
GOOGLE_API_KEY=optional_if_local

# --- App Configuration ---
PORT=8000
HOST=0.0.0.0

# --- Database ---
CHROMA_DB_PATH=./data/chroma_db
